import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { PageHero } from "@/components/page-hero"; import { prices, pricesEn, siteImages } from "@/data/content"; import { getDictionary } from "@/i18n/dictionaries"; import type { Locale } from "@/i18n/config"; export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params as any; const l=locale as Locale; const t=getDictionary(l); const groups=l==="de"?prices:pricesEn; const appointment=`/${l}/${l==="de"?"termin":"appointment"}/`; return <><PageHero script={t.pages.price.script} title={t.pages.price.title} image={siteImages.hero} alt="Beauty studio"/><section className="glory-marble py-16 sm:py-24"><div className="mx-auto w-[min(900px,calc(100%-32px))] sm:w-[min(900px,calc(100%-64px))]">{groups.map(group=><div key={group.category} className="mb-14 bg-white/85 px-6 py-7 shadow-[0_10px_30px_rgba(50,40,30,.06)] sm:px-10"><h2 className="glory-script text-4xl text-glory-gold">{group.category}</h2><div className="mt-4 border-y border-glory-line">{group.items.map(([name,price])=><div key={name} className="flex items-center justify-between gap-5 border-b border-glory-line py-5 last:border-b-0"><span className="text-sm">{name}</span><span className="font-display text-xl text-glory-gold">{price}</span></div>)}</div></div>)}<div className="text-center"><Link href={appointment} className="glory-dark-cta inline-flex items-center gap-2 px-7 py-4 text-[10px] font-bold uppercase tracking-[.16em]">{t.buttons.book}<ArrowRight size={14}/></Link></div></div></section></>;}