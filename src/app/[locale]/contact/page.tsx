import { Mail, MapPin, Phone } from "lucide-react"; import { PageHero } from "@/components/page-hero"; import { ImagePlaceholder } from "@/components/image-placeholder"; import { ScrollReveal } from "@/components/scroll-reveal"; import { openingHours, openingHoursEn, site, siteImages } from "@/data/content"; import { getDictionary } from "@/i18n/dictionaries"; import type { Locale } from "@/i18n/config"; export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params as any; const l=locale as Locale; const t=getDictionary(l); const hours=l==="de"?openingHours:openingHoursEn; return <><PageHero script={t.pages.contact.script} title={t.pages.contact.title} image={siteImages.contact} alt="Zem Beauty Studio"/><section className="bg-white py-20 sm:py-28"><div className="mx-auto grid w-[min(1120px,calc(100%-32px))] gap-12 sm:w-[min(1120px,calc(100%-64px))] lg:grid-cols-2"><ScrollReveal><div className="grid gap-7"><div className="flex gap-4"><MapPin className="mt-1 text-glory-gold" size={18}/><div><p className="glory-kicker">{t.pages.contact.address}</p><p className="mt-2 text-sm leading-6 text-glory-muted">{site.address}<br/>{site.city}</p></div></div><div className="flex gap-4"><Phone className="mt-1 text-glory-gold" size={18}/><div><p className="glory-kicker">{t.pages.contact.phone}</p><a href={`tel:${site.phone}`} className="mt-2 block text-sm text-glory-muted">{site.phone}</a></div></div><div className="flex gap-4"><Mail className="mt-1 text-glory-gold" size={18}/><div><p className="glory-kicker">{t.pages.contact.email}</p><a href={`mailto:${site.email}`} className="mt-2 block text-sm text-glory-muted">{site.email}</a></div></div></div><div className="mt-12 border-t border-glory-line pt-7"><p className="glory-kicker text-glory-gold">{t.pages.contact.hours}</p>{t.days.map((day,i)=><div key={day} className="flex justify-between border-b border-glory-line py-3 text-sm"><span>{day}</span><span className="text-glory-muted">{hours[i]}</span></div>)}</div></ScrollReveal><ScrollReveal delay={120}><ImagePlaceholder src={siteImages.contact} alt="Zem Beauty Studio" className="aspect-[4/3] lg:aspect-auto lg:min-h-[560px]"/></ScrollReveal></div></section></>;}