import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { PageHero } from "@/components/page-hero"; import { openingHours, openingHoursEn, site, siteImages } from "@/data/content"; import { getDictionary } from "@/i18n/dictionaries"; import type { Locale } from "@/i18n/config"; export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params as any; const l=locale as Locale; const t=getDictionary(l); const hours=l==="de"?openingHours:openingHoursEn; return <><PageHero script={t.pages.appointment.script} title={t.pages.appointment.title} image={siteImages.booking} alt="Beauty studio"/><section className="relative overflow-hidden bg-glory-black py-20 text-white sm:py-28"><div className="absolute inset-0 opacity-25"><div className="h-full w-full bg-cover bg-center" style={{backgroundImage:`url(${siteImages.booking})`}}/></div><div className="absolute inset-0 bg-black/70"/><div className="relative mx-auto grid w-[min(1000px,calc(100%-32px))] gap-12 lg:grid-cols-[1fr_.8fr]"><div><p className="glory-script text-5xl text-glory-gold">{t.pages.appointment.script}</p><p className="mt-6 max-w-xl text-sm leading-8 text-white/75">{t.pages.appointment.intro}</p><Link href={site.whatsapp} target="_blank" className="glory-cta mt-8 inline-flex items-center gap-2 bg-glory-gold px-7 py-4 text-[10px] font-bold uppercase tracking-[.16em]">{t.buttons.book}<ArrowRight size={14}/></Link></div><div className="border border-white/10 bg-black/30 p-7 sm:p-9"><p className="glory-kicker text-glory-gold">{l==="de"?"Öffnungszeiten":"Opening hours"}</p>{t.days.map((day,i)=><div key={day} className="flex justify-between border-b border-white/15 py-4 text-sm"><span>{day}</span><span className="text-white/70">{hours[i]}</span></div>)}</div></div></section></>;}